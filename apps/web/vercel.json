{
  "version": 2,
  "buildCommand": "bash scripts/pre-deploy.sh && next build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": [
    "sin1"
  ],
  "crons": [
    {
      "path": "/api/cron/master?secret=${MASTER_CRON_SECRET}",
      "schedule": "0 0 * * *"
    }
  ]
}