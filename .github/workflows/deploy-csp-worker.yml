name: Deploy CSP Fix Worker

on:
  push:
    branches:
      - main
    paths:
      - "workers/csp-fix/**"
  workflow_dispatch:

jobs:
  deploy-worker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Wrangler
        run: npm install -g wrangler

      - name: Deploy to Cloudflare (Zone Worker)
        run: |
          cd workers/csp-fix
          # Deploy as zone-scoped Worker
          wrangler deploy --zone-id=385c3968142891b9aa4fa5e6de6d89e9
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
