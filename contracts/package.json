{
  "name": "ploky-contracts",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:amoy": "npx hardhat run scripts/deploy-open-source.ts --network amoy",
    "test": "npx hardhat test",
    "verify:amoy": "npx hardhat verify --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.0",
    "hardhat": "^2.19.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}
